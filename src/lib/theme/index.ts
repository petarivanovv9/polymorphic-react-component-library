import type { ExampleUITheme } from './types';

import { badge } from './badge';
import { button } from './button';

export type { ExampleUITheme };
export const theme: ExampleUITheme = {
  badge,
  button,
};
